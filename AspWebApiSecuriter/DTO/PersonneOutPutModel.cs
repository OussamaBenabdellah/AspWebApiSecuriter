namespace AspWebApiSecuriter.DTO
{
    public record PersonneOutPutModel(
            string Id,
            string FullName,
            DateTime? Birthday,
            string? Address);
    
    
}
